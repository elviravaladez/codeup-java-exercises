package oopZoo;

public interface Greeter {
    //specify instance method named say hello that accepts no arguments and returns a string
    public String sayHello();
}

//Interfaces have to be abstract and don't have a body